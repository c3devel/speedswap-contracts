{
  "name": "speedswap-contracts",
  "version": "0.0.1",
  "description": "Speedswap contracts",
  "main": "index.js",
  "scripts": {
    "deploy": "npx hardhat deploy",
    "deploy:autobahn-sepolia": "npx hardhat deploy --network autobahn-sepolia",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "hardhat": "^2.19.1",
    "hardhat-deploy": "^0.11.43"
  }
}
